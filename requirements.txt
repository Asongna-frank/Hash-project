# HASH Backend — direct runtime dependencies only.
# Transitive packages are resolved automatically by pip on install.

# --- Web framework / ASGI server ---
fastapi==0.136.1
uvicorn==0.47.0

# --- Database (PostgreSQL via Supabase) ---
SQLAlchemy==2.0.49
psycopg2-binary==2.9.12

# --- Config / validation ---
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2

# --- Auth (JWT + password hashing) ---
python-jose==3.5.0
passlib==1.7.4
bcrypt==4.0.1

# --- External services ---
groq==1.4.0            # LLM provider (local dev)
httpx==0.28.1          # HTTP client (groq transport)
twilio                 # SMS provider (choronko track)
APScheduler==3.11.2    # appointment reminder scheduler

# --- Testing ---
freezegun              # deterministic datetime in tests
